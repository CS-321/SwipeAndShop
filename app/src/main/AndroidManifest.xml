<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swipeandshop">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwipeAndShop">
        <activity android:name=".PaymentPage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingsPage" />
        </activity>
        <activity android:name=".LocationPage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingsPage" />
        </activity>
        <activity android:name=".SettingsPage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".BlockUsersPage">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SettingsPage" />
        </activity>
        <activity android:name=".ProductPage"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".ChatPage"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".FeedPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".CreateAccountPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>